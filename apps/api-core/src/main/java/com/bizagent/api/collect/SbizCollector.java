package com.bizagent.api.collect;

import org.springframework.stereotype.Service;

/**
 * L1 · 상권 (소진공 상권정보 API) + 소비트렌드 대체지표(상권 매출추이로 갈음).
 * TODO(2주차): API 키 발급 후 반경 500m 경쟁강도·유동인구 수집
 */
@Service
public class SbizCollector {
    public int collect() { return 0; }
}
