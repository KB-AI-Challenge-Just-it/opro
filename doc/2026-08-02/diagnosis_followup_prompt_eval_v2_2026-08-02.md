# 진단(콜1) 재질문 프롬프트 — 2차 재검증 (2026-08-02, v1 대비)

이전 문서(`diagnosis_followup_prompt_eval_2026-08-02.md`, `run_id=20260802T072849Z`)에서 찾은 문제
두 가지에 대해 조치를 하고 같은 골든 프로필 8종으로 다시 실제 Opus를 호출한 기록이다. HARD/SOFT
용어 정의는 v1 문서 참고 — 여기서는 반복하지 않는다.

## 이번에 바뀐 것

1. **`diagnosis.py` SYSTEM 프롬프트에 길이 제약 추가** — "diagnosis는 3~5문장, 500자 이내로 압축해서
   쓰세요"를 진단 작성 규칙에 새로 넣었다. v1에서 확인한 파싱 실패(문자열 내부에 낀 불필요한 `]`로
   JSON이 깨진 사고)가 본문이 길어질수록 구조로 복귀하는 지점이 늘어나 생길 가능성이 있다고 보고,
   본문을 짧게 강제하면 그 확률이 줄어들 것이라는 가설을 세웠다(프롬프트 튜닝만으로는 확률을 낮출 뿐
   구조적으로 0%를 보장하지는 못한다는 점은 v1에서 이미 확인).
2. **`profile_facts`를 실제로 채워서 호출** — `ProfileFacts.java`를 그대로 옮긴
   `scripts/profile_facts_port.py`를 만들어, v1에서는 비워뒀던 `profile_facts` 인자를 이번엔 실제
   콜1과 동일한 형태로 채워 넘겼다. `golden_profiles.py`에도 온보딩 규칙과 같은 방식으로 파생시킨
   `revenue_basis`를 추가해 팩트시트가 실제 상황과 어긋나지 않게 했다.

호출 방식(모델·`econ_context`·저장 위치·재현 명령)은 v1과 동일하다.

## 결과

| 프로필 | 재질문 개수 | HARD | SOFT | 파싱 실패 | diagnosis 길이 |
| --- | --- | --- | --- | --- | --- |
| cafe_full | 4 | 0 | 1 | 아니오 | 336자 |
| restaurant_minimal | 4 | 0 | 0 | 아니오 | — |
| manufacturing_delinquent | 4 | 0 | 4 | 아니오 | — |
| startup_early | 4 | 0 | 1 | 아니오 | — |
| retail_unknown_finance | 4 | 0 | 0 | 아니오 | — |
| lodging_high_revenue | 4 | 0 | 3 | 아니오 | — |
| education_no_region_detail | 4 | 0 | 1 | 아니오 | — |
| etc_industry_recovering | 4 | 0 | 2 | 아니오 | 293자 |

## v1 대비 비교

| 지표 | v1 (`...49Z`) | v2 (`...05Z`, 이번) | 변화 |
| --- | --- | --- | --- |
| 파싱 실패 | 1/8 (12.5%) | **0/8 (0%)** | 개선 |
| HARD 위반 | 0/8 | 0/8 | 동일 |
| SOFT 플래그 | 4/8 | 6/8 | **증가** |
| 평균 호출 시간 | 약 19.9초 | 약 13.8초 | 단축 |

## 분석 — 개선된 것과, 그대로 안심하면 안 되는 것

**파싱 실패는 이번엔 0건이었다.** `cafe_full`(336자), `etc_industry_recovering`(293자) 등 실제로
diagnosis 본문이 500자 제약 안에서 훨씬 짧게 나왔고(v1에서 파싱이 깨졌던 케이스는 본문이 800자를
넘었다), 호출 시간도 평균 6초가량 줄었다 — 본문이 짧아졌다는 방증이다. 다만 **표본이 8건뿐이라 이번
0%가 "완전히 해결됐다"는 증거는 아니다.** v1도 8건 중 1건이 걸린 것뿐이라, 이번에 우연히 안 걸렸을
가능성을 배제할 수 없다 — 지난번에 제안했던 반복 호출(`--repeats`)로 표본을 늘려야 신뢰할 수 있는
수치가 된다.

**SOFT는 4건→6건으로 늘었지만, 이게 진짜 나빠진 신호는 아닌 것으로 보인다.** 이번에 뜬 SOFT 플래그의
절반 이상이 "매출" 키워드 관련이었다 — "최근 6개월 매출 흐름은 어떤가요?", "계절별 매출 편차가
얼마나 큰지", "대출 원리금 등 고정지출을 빼고 나면 여유가 어느 정도인가" 같은 질문들인데, 이건
`monthly_revenue_band`(구간값)를 다시 묻는 게 아니라 **추이·변동성·가처분 여력처럼 구간값만으로는
알 수 없는 다른 정보**를 캐묻는 것이다. v1의 "문제 2" 분석과 같은 패턴의 오탐이다. `profile_facts`를
이번에 새로 채워 넣은 것도 한 원인일 수 있다 — 매출 라벨이 더 선명해지면서 모델이 매출 관련 후속
질문을 더 적극적으로 만들었을 가능성이 있는데, 이건 이번 8건만으로는 원인을 분리할 수 없다.

**여전히 남은 진짜 문제**: `startup_early`에서 "지금 사장님이 하시는 서비스업은 어떤 업종에 가장
가깝나요?"가 나왔다. `industry="서비스업"`은 채워져 있지만 "기타"처럼 실질 정보가 거의 없는
포괄 카테고리라, 이건 v1에서 지적한 `_is_filled()`의 한계("기타"만 예외 처리가 필요한 게 아니라
"서비스업"처럼 넓은 카테고리도 마찬가지)가 그대로 재현된 것이다. `etc_industry_recovering`의
"어떤 업종으로 사업을 하고 계신지"도 동일 패턴. 이건 프롬프트가 아니라 **채점 코드
(`diagnosis_rules.py`)를 고쳐야 하는 문제**라는 v1의 결론이 이번에도 유지된다.

## 결론

- 길이 제약 추가는 방향이 맞았던 것으로 보인다(파싱 실패 0건, 응답 시간 단축) — 다만 8건 표본으로는
  확정할 수 없다.
- SOFT 증가는 회귀가 아니라 대부분 휴리스틱 오탐으로 보이지만, 이 역시 표본이 작아 단정하기 이르다.
- `_is_filled()`가 "기타"뿐 아니라 넓은 카테고리 값 전반에 약하다는 게 재확인됐다 — 다음으로 고칠
  우선순위가 이쪽으로 좁혀졌다.

## 다음 액션 후보

1. `--repeats`로 표본을 늘려 파싱 실패 0%·SOFT 증가가 우연이 아닌지 통계적으로 재확인.
2. `diagnosis_rules.py`의 `_is_filled()`를 "기타" 단일 예외가 아니라, 포괄 카테고리 값 일반에 대응하도록
   보강(또는 재질문이 "구체적으로/어떤 종류의" 같은 세분화 요청인지 판별하는 별도 규칙 추가).
3. SOFT 오탐이 "매출" 키워드에 쏠려 있으므로, 매출 관련 키워드 매칭에 "추이/흐름/편차/여력" 같은
   세분화 질문 패턴을 예외로 빼는 정밀도 개선을 우선 검토.

## 원본

`apps/ai-engine/scripts/output/20260802T080005Z/*.json`
